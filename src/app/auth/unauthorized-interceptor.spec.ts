import { UnauthorizedInterceptor } from './unauthorized-interceptor';

describe('UnauthorizedInterceptor', () => {
  it('should create an instance', () => {
    expect(new UnauthorizedInterceptor()).toBeTruthy();
  });
});
